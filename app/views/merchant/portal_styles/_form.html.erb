<%= simple_form_for(:merchant, @portal_style) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :btn_style %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
